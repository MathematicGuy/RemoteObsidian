A financial firm requires that any network packet destined for its web instances must be evaluated by a firewall rule that explicitly denies access to a known list of malicious IP ranges. This rule must evaluate traffic before it enters the subnet. Which security feature should be configured?
-> "explicitly denies access to a known list of malicious IP ranges." -> NACL

**Tracking (Logging) Service**
+ ins